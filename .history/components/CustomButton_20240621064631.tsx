import React from 'react'
import Image from 'next/image'

const CustomButton = () => {
  return (
    <button
    disabled={true} type={"button"} className={`custom-btn`}
    onClick={() => {}}
    >
      
    </button>
  )
}

export default CustomButton
